<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Security rule for portal users to access their own timesheets -->
    <record id="portal_timesheet_rule" model="ir.rule">
        <field name="name">Portal User Timesheet Access</field>
        <field name="model_id" ref="account.model_account_analytic_line"/>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
</odoo>